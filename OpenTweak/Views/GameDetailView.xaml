<UserControl x:Class="OpenTweak.Views.GameDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:OpenTweak.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800"
             Background="Transparent">

    <UserControl.DataContext>
        <vm:GameDetailViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Game Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Game.Name}"
                           FontSize="32"
                           FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <ui:Badge Content="{Binding Game.LauncherType}"
                              Appearance="Primary"
                              Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Game.InstallPath}"
                               Opacity="0.6"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button Content="Refresh Wiki"
                           Icon="{ui:SymbolIcon ArrowSync24}"
                           Command="{Binding FetchWikiTweaksCommand}"
                           Margin="0,0,8,0"/>
            </StackPanel>
        </Grid>

        <!-- Tabs for Tweaks and Snapshots -->
        <TabControl Grid.Row="1"
                    Background="Transparent"
                    BorderThickness="0">

            <!-- Tweaks Tab -->
            <TabItem Header="Available Tweaks">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Tweak Actions -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,8,0,8">
                        <ui:Button Content="Enable All"
                                   Command="{Binding EnableAllTweaksCommand}"
                                   Margin="0,0,8,0"/>
                        <ui:Button Content="Disable All"
                                   Command="{Binding DisableAllTweaksCommand}"/>
                    </StackPanel>

                    <!-- Tweaks List -->
                    <ListView Grid.Row="1"
                              ItemsSource="{Binding Tweaks}"
                              Background="Transparent"
                              BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{ui:ThemeResource ControlFillColorDefaultBrush}"
                                        CornerRadius="8"
                                        Padding="16"
                                        Margin="0,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <CheckBox Grid.Column="0"
                                                  IsChecked="{Binding IsEnabled}"
                                                  VerticalAlignment="Center"/>

                                        <StackPanel Grid.Column="1" Margin="12,0">
                                            <TextBlock Text="{Binding Description}"
                                                       FontWeight="SemiBold"
                                                       TextWrapping="Wrap"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                <ui:Badge Content="{Binding Category}"
                                                          Appearance="Secondary"
                                                          Margin="0,0,8,0"/>
                                                <ui:Badge Content="{Binding RiskLevel}"
                                                          Margin="0,0,8,0"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding FilePath}"
                                                       FontSize="11"
                                                       Opacity="0.5"
                                                       Margin="0,4,0,0"/>
                                        </StackPanel>

                                        <ui:Button Grid.Column="2"
                                                   Content="View on PCGW"
                                                   Icon="{ui:SymbolIcon Open24}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Empty State -->
                    <Border Grid.Row="1"
                            Visibility="{Binding Tweaks.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <ui:SymbolIcon Symbol="Document24" FontSize="48" Opacity="0.3"/>
                            <TextBlock Text="No tweaks found"
                                       FontSize="16"
                                       Opacity="0.6"
                                       Margin="0,12,0,0"/>
                            <ui:Button Content="Fetch from PCGW"
                                       Command="{Binding FetchWikiTweaksCommand}"
                                       Appearance="Primary"
                                       Margin="0,16,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Snapshots Tab -->
            <TabItem Header="Backup Snapshots">
                <ListView ItemsSource="{Binding Snapshots}"
                          Background="Transparent"
                          BorderThickness="0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{ui:ThemeResource ControlFillColorDefaultBrush}"
                                    CornerRadius="8"
                                    Padding="16"
                                    Margin="0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontWeight="SemiBold">
                                            <Run Text="Backup from "/>
                                            <Run Text="{Binding Timestamp, StringFormat='{}{0:g}'}"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Description}"
                                                   Opacity="0.7"
                                                   Margin="0,4,0,0"/>
                                        <TextBlock Opacity="0.5" FontSize="11" Margin="0,4,0,0">
                                            <Run Text="{Binding FilesBackedUp.Count}"/>
                                            <Run Text=" files backed up"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <ui:Button Content="Restore"
                                                   Icon="{ui:SymbolIcon ArrowUndo24}"
                                                   Command="{Binding DataContext.RestoreSnapshotCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                   CommandParameter="{Binding}"
                                                   Appearance="Primary"
                                                   Margin="0,0,8,0"/>
                                        <ui:Button Content="Delete"
                                                   Icon="{ui:SymbolIcon Delete24}"
                                                   Command="{Binding DataContext.DeleteSnapshotCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                   CommandParameter="{Binding}"
                                                   Appearance="Danger"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>
        </TabControl>

        <!-- Diff Preview Panel -->
        <Border Grid.Row="2"
                Background="{ui:ThemeResource ControlFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="16"
                Margin="0,8,0,8"
                Visibility="{Binding ShowDiffPreview, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" MaxHeight="200" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Preview Changes"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>

                <ListView Grid.Row="1"
                          ItemsSource="{Binding PendingChanges}"
                          Background="Transparent">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Key}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding CurrentValue}"
                                           Foreground="#FF6B6B" TextDecorations="Strikethrough"/>
                                <TextBlock Grid.Column="2" Text="{Binding NewValue}"
                                           Foreground="#51CF66"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Action Bar -->
        <Border Grid.Row="3"
                Background="{ui:ThemeResource ControlFillColorTertiaryBrush}"
                CornerRadius="8"
                Padding="16">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding StatusMessage}"
                               VerticalAlignment="Center"
                               Opacity="0.8"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Content="Preview Changes"
                               Icon="{ui:SymbolIcon Eye24}"
                               Command="{Binding PreviewChangesCommand}"
                               Margin="0,0,8,0"/>
                    <ui:Button Content="Optimize Game"
                               Icon="{ui:SymbolIcon Rocket24}"
                               Command="{Binding ApplyTweaksCommand}"
                               Appearance="Primary"
                               IsEnabled="{Binding IsApplying, Converter={StaticResource InverseBoolConverter}}"
                               FontSize="16"
                               Padding="24,12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="4"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:ProgressRing IsIndeterminate="True" Width="64" Height="64"/>
                <TextBlock Text="Loading..."
                           Margin="0,16,0,0"
                           FontSize="16"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
</UserControl>
